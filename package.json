{
  "name": "web-evaluation",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "web-dev-server --open index.html --node-resolve --watch --port 4001",
    "start:api": "json-server --watch db/db.json --port 4002",
    "test": "web-test-runner \"src/**/*.test.js\" --coverage --node-resolve"
  },
  "devDependencies": {
    "@open-wc/testing": "^3.0.0-next.5",
    "@web/dev-server": "^0.4.1",
    "@web/test-runner": "^0.18.0",
    "json-server": "^1.0.0-alpha.16"
  }
}
